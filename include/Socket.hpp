#ifndef SOCKET_HPP
#define SOCKET_HPP

#include <sys/socket.h>

class socke
{
    public:
        Socket(int domain, int type, int protocol);
        //SOCK_STREAM type = (virtual circuit service)
};

#endif